---
import Icon from '@/components/icon.astro'

interface Props {
  name: string
  highlight?: boolean
  comment?: string
}

const { name, highlight = false, comment } = Astro.props
---

<li>
  <span
    class:list={[
      'inline-flex items-center gap-1.5 rounded px-1.5 py-0.5 whitespace-nowrap',
      highlight && 'bg-primary/10 text-primary',
    ]}
  >
    <span class="size-3 shrink-0" aria-hidden="true"></span>
    <Icon
      name="file"
      class:list={['size-4 shrink-0', highlight ? 'text-primary' : 'text-muted-foreground']}
    />
    <span class="font-mono">{name}</span>
    {comment && <span class="text-muted-foreground/75 ml-2">{comment}</span>}
  </span>
</li>
